import { AlertTriangle, CheckCircle2, XCircle } from 'lucide-react'
import Card from '../../../ui/components/Card'
const mapRows=[['Документы','documents'],['Процессы','processes'],['Системы','systems'],['Роли','roles'],['Интеграции','integrations'],['KPI','kpi'],['BPMN','bpmn'],['SLA','sla'],['Ограничения','constraints']] as const
export default function KnowledgeCoverageCard({coverage}:{coverage:any}){return <Card title='Покрытие знаний'><div className='coverage-list'>{mapRows.map(([l,k])=>{const ok=Boolean(coverage?.[k]); const s=ok?'success':'warning'; return <div className='coverage-item' key={l}><span className='coverage-left'>{ok?<CheckCircle2 size={16} className='ok'/>:<AlertTriangle size={16} className='warn'/>}<span className='coverage-label'>{l}</span></span><span className={`coverage-status ui-badge ${s==='success'?'ready':'warning'}`}>{ok?'OK':'Нет данных'}</span></div>})}</div></Card>}
