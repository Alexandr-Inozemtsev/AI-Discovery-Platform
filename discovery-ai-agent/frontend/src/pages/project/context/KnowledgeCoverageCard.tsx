import { AlertTriangle, CheckCircle2, XCircle } from 'lucide-react'
import Card from '../../../ui/components/Card'
export default function KnowledgeCoverageCard({coverage,sourceTrace}:{coverage:any;sourceTrace:any[]}){const docs=sourceTrace.filter((s:any)=>s.source_type==='document'); const withText=docs.filter((d:any)=>d.content_level!=='metadata_only').length; const docsStatus=docs.length===0?'Нет данных':withText===0?'Только метаданные':withText<docs.length?'Частично':'OK'; const notEnoughText=docs.length>0&&withText===0; const rows=[['Документы',docsStatus,docsStatus==='OK'?'ready':'warning'],['Процессы',coverage?.processes?'OK':(notEnoughText?'Недостаточно текста':'Нет данных'),coverage?.processes?'ready':'warning'],['Системы',coverage?.systems?'OK':(notEnoughText?'Недостаточно текста':'Нет данных'),coverage?.systems?'ready':'warning'],['Участники / ответственные',coverage?.roles?'OK':'Нет данных',coverage?.roles?'ready':'warning'],['Интеграции',coverage?.integrations?'OK':(notEnoughText?'Недостаточно текста':'Нет данных'),coverage?.integrations?'ready':'warning'],['Метрики / KPI',coverage?.kpi?'OK':'Нет данных',coverage?.kpi?'ready':'warning'],['BPMN',coverage?.bpmn?'OK':'Нет BPMN',coverage?.bpmn?'ready':'warning'],['SLA',coverage?.sla?'OK':'Нет SLA',coverage?.sla?'ready':'warning'],['Ограничения',coverage?.constraints?'OK':'Не указаны',coverage?.constraints?'ready':'warning']] as const; const hints=[] as string[]; if(notEnoughText) hints.push('Извлеките текст из документов'); if(!coverage?.bpmn) hints.push('Добавьте BPMN текущего процесса'); if(!coverage?.kpi) hints.push('Уточните KPI и целевые метрики'); if(!coverage?.constraints) hints.push('Добавьте ограничения и допущения'); if(!coverage?.systems||!coverage?.processes) hints.push('Добавьте описание процесса или документы с извлечённым текстом'); return <Card title='Покрытие знаний'><div className='coverage-list'>{rows.map(([l,status,badge])=><div className='coverage-item' key={l}><span className='coverage-left'>{status==='OK'?<CheckCircle2 size={16} className='ok'/>:status.includes('Нет')?<XCircle size={16} className='bad'/>:<AlertTriangle size={16} className='warn'/>}<span className='coverage-label'>{l}</span></span><span className={`coverage-status ui-badge ${badge}`}>{status}</span></div>)}</div>{hints.length>0&&<div className='hint-card'><b>Что ещё можно добавить</b><ul>{hints.slice(0,5).map((h,i)=><li key={i}>{h}</li>)}</ul></div>}</Card>}
