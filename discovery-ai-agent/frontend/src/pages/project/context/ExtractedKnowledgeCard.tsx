import Card from '../../../ui/components/Card'
import Button from '../../../ui/components/Button'
const groups:[string,string[]][]=[['Процессы',['процессы','processes']],['Системы',['системы','systems']],['Роли',['роли','roles']],['Интеграции',['интеграции','integrations']],['Ключевые сущности',['бизнес_сущности','business_entities']],['Документы',['документы','documents']],['Термины',['термины','terms']]]
export default function ExtractedKnowledgeCard({knowledge,onRefresh}:{knowledge:any;onRefresh:()=>void}){return <Card title='Извлечённые знания (AI)' actions={<Button variant='soft' size='sm' onClick={onRefresh}>Обновить извлечение</Button>}>{!knowledge?<p className='sub'>AI пока не извлёк знания. Нажмите «Обновить извлечение».</p>:groups.map(([label,keys])=>{const items=keys.flatMap(k=>Array.isArray(knowledge?.[k])?knowledge[k]:[]);return <div key={label} className='knowledge-group'><div className='knowledge-group__title'>{label} ({items.length})</div><div className='knowledge-pills'>{items.slice(0,8).map((x:any,i:number)=><span className='knowledge-pill' key={i}>{String(x)}</span>)}</div></div>})}</Card>}
